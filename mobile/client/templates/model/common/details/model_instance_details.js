Template.model_instance_details_phone.helpers({
    fields:function(){
        // assuming current model references current model definition
        return ModelHelpers.currentModel().fields;
    }
});
